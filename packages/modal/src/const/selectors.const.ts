/** Selector del `<dialog>` que contiene el modal. */
export const MODAL_SEL = '#dialog-form'

/** Selector del contenedor donde se inyecta la vista parcial. */
export const MODAL_BODY_SEL = '#dialog-form #modal-body'
